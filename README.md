## How To Run
1. Install dependencies:
   `npm install`
2. Start development server:
   `npm run dev`

## How To Run Production Build Output
1. Generate the production build:
   `npm run build`
2. Serve the built output from `dist`:
   `npm run preview`


To run tests: `npm run test`
